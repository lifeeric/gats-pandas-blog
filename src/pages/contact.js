import React from 'react';
import Layout from '../components/layout';

export default () => {
    return (
        <Layout>
            <h1>I'd love to talk! Email at the address below:</h1>
            <p>
                <a href="mailto:me@example">me@example.com</a>
            </p>
        </Layout>
    );
}